// select the element or group of element the we want
// decide the effect we want to apply to the selection

// getElementById('element)

const h1 = document.getElementById('title');
h1.style.color = 'red';      //     'id'

// accessing and updating directly
// document.getElementById('btn').style.backgroundColor = 'blue';

// document.getElementById('btn').style.color = "white";

// or can be s=done like this

const btn = document.getElementById('btn');
btn.style.backgroundColor = "blue";    // changing background color
btn.style.color = "white";     // changing text color